# 🚀 部署步骤 - 已推送到 GitHub

## ✅ 当前状态

✅ 代码已成功推送到：**https://github.com/binbin1213/weekly**

---

## 🎯 方式 1：GitHub Pages 部署（推荐新手）

### 步骤（只需 3 分钟）

1. **访问你的 GitHub 仓库**
   ```
   https://github.com/binbin1213/weekly
   ```

2. **进入设置**
   - 点击仓库顶部的 **Settings**（设置）
   - 在左侧菜单找到 **Pages**

3. **配置部署源**
   - **Source** 选择：**GitHub Actions**
   - 点击 **Save**（保存）

4. **查看部署进度**
   - 返回仓库首页
   - 点击顶部的 **Actions** 标签
   - 等待 "部署网站" 工作流运行完成（约 2-3 分钟）
   - ✅ 显示绿色勾号表示成功

5. **访问你的网站**
   ```
   https://binbin1213.github.io/weekly/
   ```

**就这么简单！** 🎉

---

## 🚀 方式 2：Vercel 部署（最快最优）

### 为什么选择 Vercel？
- ⚡ 全球 CDN，速度最快
- 🔒 自动 HTTPS
- 🆓 免费额度充足
- 📱 完美支持自定义域名

### 步骤（5 分钟）

#### 方法 A：网页部署（最简单）

1. **访问 Vercel**
   ```
   https://vercel.com/
   ```

2. **导入项目**
   - 点击 **Add New** > **Project**
   - 选择 **Import Git Repository**
   - 选择 `binbin1213/weekly`

3. **配置项目**
   - **Framework Preset**: 自动检测为 VitePress
   - **Build Command**: `npm run build && npm run generate-rss`
   - **Output Directory**: `.vitepress/dist`
   - 点击 **Deploy**

4. **等待部署**
   - 约 1-2 分钟
   - ✅ 完成后会显示网址

5. **访问网站**
   - 类似：`https://weekly-xxx.vercel.app`

#### 方法 B：命令行部署（更快）

```bash
# 1. 安装 Vercel CLI（如果还没安装）
npm install -g vercel

# 2. 登录 Vercel
vercel login
# 会打开浏览器进行登录

# 3. 部署到预览环境
vercel

# 4. 部署到生产环境
vercel --prod
```

**完成！** 你会得到一个网址 🎉

---

## 🔄 自动同步已启用

你的 GitHub Actions 已经配置好了：

### ✅ 自动同步功能
- ⏰ **每周五**自动检查阮一峰老师的原仓库（与发布时间同步）
- 🔄 有新文章时自动合并到你的仓库
- 🚀 自动触发网站重新部署
- 📧 失败时会收到邮件通知

### 🔍 查看同步状态

1. 进入你的 GitHub 仓库
   ```
   https://github.com/binbin1213/weekly
   ```

2. 点击顶部的 **Actions** 标签

3. 应该能看到三个工作流：
   - ✅ **同步上游仓库** - 每周五运行
   - ✅ **部署网站** - 每次推送运行
   - ✅ **部署到 Vercel**（如果启用）

### 🎮 手动触发同步（可选）

如果想立即同步最新文章：

1. 进入 Actions 标签
2. 点击左侧的 **同步上游仓库**
3. 点击右侧的 **Run workflow**
4. 点击绿色的 **Run workflow** 按钮

---

## 📡 RSS 订阅地址

部署完成后，你的 RSS 订阅地址：

### GitHub Pages
```
RSS 2.0:   https://binbin1213.github.io/weekly/feed.xml
Atom:      https://binbin1213.github.io/weekly/atom.xml
JSON Feed: https://binbin1213.github.io/weekly/feed.json
```

### Vercel（如果使用）
```
RSS 2.0:   https://your-domain.vercel.app/feed.xml
Atom:      https://your-domain.vercel.app/atom.xml
JSON Feed: https://your-domain.vercel.app/feed.json
```

### 测试 RSS

1. 复制 feed.xml 的完整网址
2. 在 RSS 阅读器中添加订阅
3. 应该能看到最新 20 篇周刊

**推荐的 RSS 阅读器**：
- 📱 iOS: NetNewsWire, Reeder
- 🤖 Android: Feedly, Inoreader  
- 💻 Web: Feedly, Inoreader

---

## ✅ 部署检查清单

部署完成后，请检查：

### 网站功能
- [ ] 首页正常显示
- [ ] 可以打开第 368 期（最新）
- [ ] 可以打开第 1 期（最早）
- [ ] 搜索功能正常（输入关键词测试）
- [ ] 侧边栏可以展开/折叠
- [ ] 深色模式切换正常（右上角切换按钮）
- [ ] 移动端显示正常（用手机打开测试）

### RSS 订阅
- [ ] `/feed.xml` 可以访问
- [ ] `/atom.xml` 可以访问  
- [ ] `/feed.json` 可以访问
- [ ] RSS 阅读器可以成功订阅

### 自动化功能
- [ ] Actions 中有三个工作流
- [ ] "部署网站" 工作流运行成功
- [ ] "同步上游仓库" 工作流已启用

---

## 🎨 个性化定制（可选）

### 1. 修改网站标题

编辑 `.vitepress/config.ts`：

```typescript
title: '你的网站标题',           // 改这里
description: '你的网站描述',      // 改这里
```

### 2. 修改主题颜色

编辑 `.vitepress/theme/custom.css`：

```css
:root {
  --vp-c-brand: #646cff;        /* 主色调 */
  --vp-c-brand-light: #747bff;  /* 浅色 */
  --vp-c-brand-dark: #535bf2;   /* 深色 */
}
```

### 3. 修改首页内容

编辑 `index.md`

### 4. 更换 Logo

替换 `public/logo.svg`

### 提交修改

```bash
git add .
git commit -m "chore: 个性化配置"
git push
```

推送后会自动重新部署！

---

## 🌍 配置自定义域名（可选）

### GitHub Pages

1. 在 Pages 设置页面
2. **Custom domain** 输入你的域名（如：`weekly.yourdomain.com`）
3. 在域名 DNS 设置中添加 CNAME 记录：
   ```
   类型: CNAME
   名称: weekly
   值: binbin1213.github.io
   ```

### Vercel

1. 在 Vercel Dashboard 进入项目
2. **Settings** > **Domains**
3. 添加你的域名
4. 按照提示配置 DNS

---

## 📊 添加访问统计（可选）

### Google Analytics

1. 获取 GA4 跟踪 ID
2. 编辑 `.vitepress/config.ts`：

```typescript
head: [
  ['script', { 
    async: '', 
    src: 'https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX' 
  }],
  ['script', {}, `
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-XXXXXXXXXX');
  `]
]
```

### Vercel Analytics（如果使用 Vercel）

在 Vercel Dashboard > Analytics > Enable

---

## 🐛 常见问题

### Q: 部署失败怎么办？

**A:** 查看 Actions 日志：
```
1. 进入 Actions 标签
2. 点击失败的工作流
3. 查看错误信息
4. 通常是 Node.js 版本或依赖问题
```

### Q: RSS Feed 显示 404？

**A:** 确保：
```
1. 构建命令包含 npm run generate-rss
2. .vitepress/dist/ 目录中有 feed.xml
3. 等待部署完成（约 2-3 分钟）
```

### Q: 图片不显示？

**A:** 检查：
```
1. 图片路径是否正确
2. 原仓库的图片可能使用外链
3. GitHub 图床可能需要代理访问
```

### Q: 搜索不工作？

**A:** 搜索功能：
```
1. 只在生产环境完全正常
2. 本地开发时可能不完整
3. 部署后等待索引构建（约 1 分钟）
```

### Q: 自动同步不工作？

**A:** 检查：
```
1. GitHub Actions 是否启用
2. 查看工作流运行日志
3. 确认 .github/workflows/sync-upstream.yml 存在
```

---

## 🎉 完成！

现在你有：
- ✅ 代码在 GitHub：`https://github.com/binbin1213/weekly`
- 🌐 网站已部署（选择一个方式）
- 🔄 自动同步已启用
- 📡 RSS 订阅已配置

---

## 📝 快速命令

```bash
# 本地开发
npm run dev              # 启动开发服务器

# 更新代码
git add .
git commit -m "描述"
git push                 # 推送后自动部署

# 手动同步原仓库
git remote add upstream https://github.com/ruanyf/weekly.git
git fetch upstream
git merge upstream/master
git push

# Vercel 部署
vercel --prod
```

---

## 🙏 下一步建议

1. ⭐ 给 [阮一峰老师的原仓库](https://github.com/ruanyf/weekly) 点个星
2. 🔖 收藏你的网站
3. 📡 用 RSS 阅读器订阅
4. 📢 分享给朋友

---

**祝你使用愉快！** 🎉✨

网站地址：
- GitHub Pages: `https://binbin1213.github.io/weekly/`
- Vercel: 部署后获得

